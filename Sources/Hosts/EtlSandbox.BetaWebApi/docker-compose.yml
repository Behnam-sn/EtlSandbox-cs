services:
  etl-sandbox-beta-web-api:
    build:
      context: ../../../
      dockerfile: Sources/Hosts/EtlSandbox.BetaWebApi/Dockerfile
    profiles:
      - apps
    networks:
      - network
    ports:
      - "5003:8080"
      - "5004:8081"

networks:
  network:
    driver: bridge