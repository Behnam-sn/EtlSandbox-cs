services:
  etl-sandbox-delta-web-api:
    build:
      context: ../../../
      dockerfile: Sources/Hosts/EtlSandbox.DeltaWebApi/Dockerfile
    profiles:
      - apps
    networks:
      - network
    ports:
      - "5005:8080"
      - "5006:8081"

networks:
  network:
    driver: bridge